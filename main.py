# coding: utf-8
from functions import db_update_works_info, db_update_students, db_update_total_grades, db_connection, admin_creator, wait, total_info_creator
from collections import defaultdict
import psycopg2
import threading
import time

if __name__ == '__main__':
    pass
    #threading.Thread(target=wait).start()
    #threading.Thread(target=db_update_total_grades).start()
    #threading.Thread(target=wait).start()
    #total_info_creator()
    #total_info_creator()
    #admin_creator()
    #student_name, student_login, student_password, student_row
    #db_update_total_grades()
    #db_update_total_grades()
    #db_update_works_info()
    #db_update_total_grades()
